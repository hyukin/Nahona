<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.techtwn.nahona">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_main"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_main_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".write_selling_info"/>
        <activity android:name="shopping.carrot"></activity>
        <activity android:name=".loginActivity" />
        <activity android:name=".homeActivity" />
        <activity
            android:name="shopping.ShoppingActivity"
            android:label="쇼핑" />
        <activity
            android:name=".Picture"
            android:label="상세정보" />
        <activity android:name=".Sell" />
        <activity
            android:name=".registerActivity"
            android:label="회원가입"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>